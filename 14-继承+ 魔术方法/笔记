核心概念：
    继承（Inheritance）:儿子自动拥有父亲的方法和属性
    重写：有继承的能力，单儿子想用自己的方法实现
1.单继承
    语法：class Chilid(Parent)
2.方法重写
    使用super（）在重写时保留父类的功能（扩展而不是全覆盖）


魔法方法：
    1.__init__(初始化)
          作用：构造函数，对象创建时自动调用，用于赋值
          只负责初始化属性，勿做耗时操作
    2.__str__(用户视图)
        作用：定义print（对象）或者str（对象）时显示的内容
        目标：易读，给用户看
    3.__repr__(开发者视图)
        作用：定义在控制台直接输入对象名回车，或repr（对象）时显示内容
        目标：准确，给程序员调试看。通常格式为ClassName（attr = value）
        兜底机制：如果没有定义__str__，print会自动调用__repr__

