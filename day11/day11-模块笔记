模块的作用：
    代码复用：将常用的功能封装到模块中，可以在多个程序中重复使用
    命名空间管理：模块可以避免命名冲突，不同模块中的同名函数或者变量互相不干扰
    代码组织：将代码按功能份到不同的模块中，使程序更清晰

一、想要使用python源文件，只需要在另一个源文件里执行import语句
import module1[,module2[,module3...]


二、from...import语句————从模块中导入指定部分到当前命名的空间（import后面跟*表示把全部被内容导入当前命名空间，但不推荐容易引起命名冲突）
    from modname import name1[,name2.....]
    from fibo import  fib, fib2 #导入fibo中的fib函数，并不会把整个fibo模块导入命名空间内

    给模块取别名  as
    import numpy as np #将numpy模块别设置为np

三、__name__属性
   一个模块被另一个模块程序第一次引入时，其主程序将运行，若想在引入模块时，模块中的某一块程序不执行，
   则可以使用__name__属性来使该成程序块仅在该模块自身运行时执行
   模块被直接运行，__name__的值为__main__,被导入值为“模块名”

四、dir()函数
    找到模块内定义的所有名称，以一个字符串列表形式返回

五、包（暂时未学）

calculate_mean (统计基础)
用途： 数据分析中最基础的操作。
亮点： 加入了空列表的异常处理，体现了代码的健壮性。
is_prime (算法逻辑)
用途： 加密算法、数学题解的基础。
亮点： 没有使用简单的遍历，而是使用了的优化算法，体现了对性能的关注。
euclidean_distance (几何应用)
用途： 机器学习（KNN算法）、游戏开发（计算怪物距离）、地图应用。
亮点： 使用了类型提示 Tuple，展示了处理坐标数据的能力。
factorial (递归/循环基础)
用途： 排列组合计算、概率论。
亮点： 处理了边界情况（0和1）以及错误输入（负数）。
clamp (工程实用)
用途： 这是一个非常“工程师”的函数。比如限制音量在 0-100 之间，或者限制游戏角色不跑出地图边界。
亮点： 展示了代码在实际工程中的实用性，不仅仅是数学公式